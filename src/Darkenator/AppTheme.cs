namespace Darkenator;

/// <summary>The two states Windows offers. Kept in its own file so the pieces that only
/// need the vocabulary do not have to drag in the registry and interop layer.</summary>
public enum AppTheme
{
    Light,
    Dark
}
